PlayerStatusRingUI uses 2 textures:
- UI/ring_hp.png
- UI/ring_dodge.png

Prepare textures so that their width represents fill amount (0..100%) when cropped from left.
If you want a real circular fill, implement a shader or use a pre-baked atlas.
